# Stage 3 / Stage 6 — training deps for Unsloth + QLoRA on RTX 50-series.
#
# Install ORDER matters. On RTX 5060 Ti (Blackwell, sm_120) you need
# a CUDA 12.8 build of torch >= 2.6 (older cu124 wheels lack sm_120 kernels).
#
#   pip install --upgrade pip
#   pip install torch==2.8.0 torchvision==0.23.0 torchaudio==2.8.0 \
#       --index-url https://download.pytorch.org/whl/cu128
#   pip install -r requirements-train.txt
#
# IMPORTANT: this file deliberately does NOT pin torch/torchvision/torchaudio
# so it cannot downgrade the cu128 wheels you installed above. The version
# constraints below are picked to be compatible with torch 2.8 — bumping
# xformers floor to 0.0.29 avoids the 0.0.28 wheel (which forces torch==2.5).
#
# Unsloth pulls additional wheels at runtime. If install breaks, follow
# https://github.com/unslothai/unsloth#installation-instructions for
# the exact pip line that matches your torch+cuda combo.

unsloth>=2026.4.0
unsloth_zoo>=2026.4.0

transformers>=4.48.0
trl>=0.13.0
peft>=0.13.0
datasets>=3.0.0
accelerate>=1.2.0
bitsandbytes>=0.45.0
sentencepiece>=0.2.0
safetensors>=0.4.5
# xformers wheels are tied to a specific torch version via hard pins in
# their metadata, so leave this unpinned — pip will pick the wheel that
# matches whatever torch you installed (e.g. torch 2.8 -> xformers 0.0.32).
# Do NOT pin to 0.0.28 — that wheel forces torch==2.5 and will downgrade you.
xformers; platform_system == "Linux"

# Used by save_pretrained_gguf to convert + quantize.
gguf>=0.10.0
protobuf>=4.25.0

# Multi-provider generation (Slice 2).
openai>=1.50
google-genai>=1.73.1
