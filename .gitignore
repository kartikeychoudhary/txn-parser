# Secrets / env
.env
.env.*
!.env.example
.ienv

# Python virtualenvs
.venv/
venv/
env/
__pycache__/
*.pyc
*.pyo
*.pyd

# Trained model artifacts — pulled from HF (kartikey31/txn-parser) instead
models/

# Local caches / build artifacts
unsloth_compiled_cache/
logs/

# Node
viewer/node_modules/
viewer/npm-debug.log*

# OS / editor / agent state
.DS_Store
Thumbs.db
.idea/
.vscode/
.claude/
*.swp
data/distill/metrics.json

# Test / coverage caches
.coverage
.pytest_cache/

# Local snapshots / backups (data/ itself is committed)
backup_data.*
data/*.7z
data/*.zip
data/*.bak
data/*.bak/
data/distill/*.bak
data/distill/*.pre_fix_*

# Orchestrator UI run history
orchestrator/runs/
orchestrator/node_modules/
